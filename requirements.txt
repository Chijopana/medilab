# Medilab — dependencias del proyecto
#
# Requiere Python 3.11: TensorFlow no publica wheels para Windows en 3.12+.
# Instalación:  pip install -r requirements.txt
#
# Este fichero lista solo lo que el código importa de verdad. La lista anterior
# arrastraba el stack de Jupyter, matplotlib, OpenCV, mediapipe y pyttsx3, que
# ya no usa ninguna parte del proyecto.

# --- Núcleo web ---
Django==5.2.17

# --- Generación del PDF del expediente ---
reportlab==5.0.0

# --- Tratamiento de imágenes (diagnóstico por IA) ---
pillow==10.4.0

# --- Modelos de IA: diagnóstico por imagen y chatbot ---
tensorflow-intel==2.17.0        ; sys_platform == 'win32'
tensorflow==2.17.0              ; sys_platform != 'win32'
keras==3.4.1
numpy==1.23.5
h5py==3.11.0

# --- Necesario para deserializar el label_encoder.pickle del chatbot ---
scikit-learn==1.5.1
scipy==1.14.0
joblib==1.4.2
threadpoolctl==3.5.0
